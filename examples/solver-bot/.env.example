# IC Network Configuration
IC_NETWORK=local                                    # Options: local, ic
INTENT_POOL_CANISTER_ID=rrkah-fqaaa-aaaaa-aaaaq-cai # Replace with your canister ID

# Solver Identity
SOLVER_IDENTITY_PATH=./identity.json                # Path to your solver's identity JSON file (auto-generated if not exists)

# Monitoring Configuration
POLLING_INTERVAL_MS=5000                            # How often to poll for new intents (milliseconds)

# Profitability Configuration
MIN_PROFIT_BPS=50                                   # Minimum profit in basis points (50 = 0.5%)

# Exchange Rates (Demo - Fixed Rates)
# In production, these would come from live pricing APIs
RATE_ETH_HOO=50000                                  # 1 ETH = 50000 HOO
RATE_ICP_ETH=0.002                                  # 1 ICP = 0.002 ETH
RATE_ICP_HOO=100                                    # 1 ICP = 100 HOO
RATE_HOO_ETH=0.00002                                # 1 HOO = 0.00002 ETH

# Supported Assets
SUPPORTED_CHAINS=ethereum,sepolia,hoosat,icp
SUPPORTED_TOKENS=ETH,ICP,HOO,USDC

# Solver Fee Configuration (basis points)
DEFAULT_SOLVER_FEE_BPS=30                           # 0.3% solver fee
DEFAULT_SOLVER_TIP_BPS=10                           # 0.1% solver tip
